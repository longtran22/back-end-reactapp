CourseController

{/* <div class="form-group">
                <label for="country">COUNTRY</label>
                <input type="text" class="form-control" value="{{course.country}}" id="country" name="country">
                {{!-- <small value="{{course.}}" id="emailHelp" class="form-text text-muted">We'll never share your email with anyone else.</small> --}}
            </div>
             <div class="form-group">
                <label for="count">COUNT</label>
                <input type="number" class="form-control" value="{{course.count}}" id="count" name="count">
                {{!-- <small value="{{course.}}" id="emailHelp" class="form-text text-muted">We'll never share your email with anyone else.</small> --}}
            </div>
            {{!-- <div class="form-group">
               <label for="description">Mo ta</label>
                <input type="email" class="form-control" value="{{course.}}" id="description" name="description">
                {{!-- <small value="{{course.}}" id="emailHelp" class="form-text text-muted">We'll never share your email with anyone else.</small> --}}
            </div>  */}